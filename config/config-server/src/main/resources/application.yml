spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/pangzl/config-repo.git
          # 强制拉取资源文件
          force-pull: true
#          search-paths:
#          username:
#          password:

server:
  port: 60000